#Сложность данного алгоритма O(n).
class Solution:
    def maxScoreSightseeingPair(self, values):
        cur = res = 0
        for a in values:
            res = max(res, cur + a)
            cur = max(cur, a) - 1
        return res
